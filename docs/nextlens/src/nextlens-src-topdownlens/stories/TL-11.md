---
feature: nextlens-src-topdownlens
story_id: TL-11
doc_type: story
status: done
title: GitHub Actions Pipelines
depends_on: [TL-8]
implementation_kind: cli
epic: 2
spine: false
updated_at: 2026-05-14T04:00:00Z
---

# TL-11 - GitHub Actions Pipelines

## Goal

Provide CI workflows for the self-hosting topology: contract validation, governance publish gate, and a regression+doctor pipeline.

## Scope

- Contract validation workflow (runs on PRs to control plan branches).
- Governance publish gate workflow (runs on publish-to-governance).
- Regression-and-doctor workflow (runs on dev branches; consumes TL-7 output if available).
- Workflow templates under the feature docs path, ready for later installation into `.github/workflows/` by an approved setup or publication operation.

## Acceptance

- Each workflow has a documented trigger, success criteria, and failure routing.
- Workflows do not write outside their assigned repo.
- Governance publish gate refuses to merge if metadata or required artifacts are missing.
- Regression-and-doctor workflow fails the run (does not pass) if TL-5 or TL-7 CLIs are not present; a non-publication mode must be declared explicitly rather than silently allowing the job to pass with a placeholder. Define a `--dry-run` or `SKIP_DOCTOR=true` opt-in flag that makes the skip explicit and still exits non-zero in strict mode.

## Files To Produce

- `docs/nextlens/src/nextlens-src-topdownlens/workflows/contract-validation.yml`.
- `docs/nextlens/src/nextlens-src-topdownlens/workflows/governance-publish-gate.yml`.
- `docs/nextlens/src/nextlens-src-topdownlens/workflows/regression-and-doctor.yml`.
- `docs/nextlens/src/nextlens-src-topdownlens/guides/ci-pipelines.md` (operator notes).

## Notes For Dev

- CLI/config story. Workflow YAML templates live with the feature documents; installation into repo-root `.github/workflows/` remains governed by the topology defined in TL-8.
- For the first increment, regression-and-doctor must either have real validators or be set to an explicit non-publication mode (fail closed). A placeholder that silently passes would bypass the publication guardrails this story is meant to enforce.

## Dev Agent Record

- Status: done
- Files produced: `workflows/contract-validation.yml`, `workflows/governance-publish-gate.yml`, `workflows/regression-and-doctor.yml`, and `guides/ci-pipelines.md`.
- Boundary note: root `.github/` is intentionally ignored in this control repo and is sync-generated. TL-11 therefore stores workflow YAML as feature-doc templates in the same docs location as the other TopDownLens artifacts; installing them into root `.github/workflows/` requires a later approved setup or publication operation.
- Validation: `uv run --with pyyaml python` parsed all three workflow templates and confirmed TL-11 is marked done in sprint/session state.
