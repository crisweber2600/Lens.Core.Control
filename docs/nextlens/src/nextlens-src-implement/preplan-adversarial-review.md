---
feature: nextlens-src-implement
doc_type: adversarial-review
phase: preplan
source: phase-complete
verdict: pass-with-warnings
status: responses-recorded
updated_at: 2026-05-14T00:00:00Z
review_format: abc-choice-v1
---

# Adversarial Review: nextlens-src-implement / preplan

**Reviewed:** 2026-05-14T00:00:00Z
**Source:** phase-complete
**Overall Rating:** pass-with-warnings

## Summary

The staged preplan packet is coherent and ready to hand off into businessplan. The brainstorm, research, and product brief align on the same constrained v1 path: one interactive command spine, authoritative stable-ID writes, disposable graph projection, JSONL doctor output, and deduplicated Salmon correction routing. No logic flaw was found that would block progression. The remaining concerns are handoff-shaping issues rather than phase-breaking defects: the product packet schema boundary is still underspecified, downstream implementation ownership is not yet mapped to target repos, and the cross-feature context recorded in governance is still only a stub. These should be carried into businessplan as explicit follow-ups, not treated as blockers on preplan completion.

## Findings

### Critical

None.

### High

| # | Dimension | Finding | Recommendation |
|---|-----------|---------|----------------|
| H1 | Coverage Gaps | The artifact set consistently says the flow should emit one BMAD-ready feature packet, but it does not yet define the minimum packet schema or mandatory metadata fields beyond high-level intent. That leaves businessplan exposed to re-deciding what counts as a valid packet instead of elaborating a fixed preplan contract. | Add an explicit packet contract decision in businessplan: required metadata, evidence fields, confirmation state, and how packet identity maps to stable IDs. |

### Medium / Low

| # | Dimension | Finding | Recommendation |
|---|-----------|---------|----------------|
| M1 | Complexity and Risk | `feature.yaml` still has an empty `target_repos` list. That is acceptable in preplan, but it leaves the eventual implementation ownership boundary unresolved for downstream planning and can create ambiguity when businessplan starts shaping delivery responsibilities. | Register the implementation ownership boundary by setting `feature.yaml.target_repos` or the equivalent Feature metadata before dev routing begins. This should identify the target implementation repository/path and prevent control-repo planning artifacts from being confused with implementation ownership. |
| M2 | Cross-Feature Dependencies | The governance summary for this feature is still an autogenerated stub, so the durable cross-feature context visible outside this session is minimal. That weakens discoverability for later reviewers who did not participate in the current chat history. | Refresh the feature summary during the next planning step so the core intent and constraints are visible from governance context without requiring transcript recovery. |
| L1 | Assumptions and Blind Spots | The product brief leaves two open operational thresholds undecided: minimum v1 packet metadata and when Salmon routing should escalate automatically versus remain human-confirmed. These are correctly surfaced as open questions, but if left implicit they will reappear as implementation churn. | Convert both open questions into explicit businessplan decisions or acceptance criteria. |

## Accepted Risks

- Preplan intentionally stops short of defining the full packet schema. This is accepted as a businessplan elaboration item because the current artifact set already narrows the implementation direction enough for the next phase.
- Governance summary detail remains light for now. This is accepted as documentation debt rather than a phase gate blocker.

## Party-Mode Challenge

Morgan (Delivery Lead): The packet is intentionally narrow, but what prevents businessplan from expanding it back into a general-purpose planning surface? If the v1 boundary is real, it needs a crisp contract for what is explicitly excluded.

Rina (Operations Reviewer): JSONL doctor output and idempotency are good calls, but the packet still assumes the operator can tell when a correction should stay local versus escalate through Salmon. That boundary needs to become operational guidance, not shared intuition.

Theo (Systems Architect): The authoritative-versus-projection split is sound, but the repo ownership path is still deferred. If target repo mapping stays unset too long, downstream design work may blur control-repo responsibilities with implementation responsibilities.

## Gaps You May Not Have Considered

1. What exact fields make a packet reviewable by BMAD without requiring fallback narrative context from chat history? 
2. What event classes are allowed to auto-route through Salmon, and which require explicit human approval?
3. How will operators distinguish authoritative artifacts from rebuilt projections in logs, evidence bundles, and troubleshooting output?
4. What evidence proves a replayed request reused a prior idempotent response rather than silently re-running side effects?

## Open Questions Surfaced

- What is the minimum required packet schema for v1?
- What escalation threshold governs automatic versus manual Salmon routing?
- Which target repo should own the first implementation Feature once businessplan turns intent into delivery scope?
