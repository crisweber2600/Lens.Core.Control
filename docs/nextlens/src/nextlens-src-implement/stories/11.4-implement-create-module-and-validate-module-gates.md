---
feature: nextlens-src-implement
story_id: "11.4"
doc_type: story
status: ready
title: "Implement Create Module and Validate Module Gates"
epic: "EP11: BMAD Module Packaging & Registration"
priority: High
complexity: Medium
depends_on: ["11.1", "11.2", "11.3"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 11.4: Implement Create Module and Validate Module Gates

## Context

As a **NextLens implementation team**, I want to **run Create Module (CM) and Validate Module (VM) gates before release**, so that **packaged output is verified correct**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 11.1](../stories/11.1-implement-module-identity-and-configuration-schema.md), [Story 11.2](../stories/11.2-implement-module-help-registration.md), [Story 11.3](../stories/11.3-implement-marketplace-manifest.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Create Module and Validate Module Gates`.
2. Implement the acceptance slice `Create Module (CM) Execution`.
3. Implement the acceptance slice `CM File Generation`.
4. Implement the acceptance slice `Validate Module (VM) Execution`.
5. Implement the acceptance slice `VM Validation Results`.
6. Implement the acceptance slice `VM Issue Resolution`.
7. Implement the acceptance slice `Integration with Release`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Create Module (CM) Execution
     - Given: implementation is ready for packaging
     - When: CM command runs
     - Then: CM:
       - regenerates module.yaml from current capabilities
       - regenerates module-help.csv from current skills
       - regenerates .claude-plugin/marketplace.json from current skills
       - verifies all referenced skill files exist
       - reports results (success/failure)
- [ ] CM File Generation
     - Given: CM runs
     - When: files are generated
     - Then: generated files include:
       - updated module.yaml
       - updated module-help.csv
       - updated marketplace.json
       - with checksums or timestamps for tracking
- [ ] Validate Module (VM) Execution
     - Given: CM completes successfully
     - When: VM command runs
     - Then: VM:
       - validates module.yaml schema
       - validates module-help.csv format and consistency
       - validates marketplace.json completeness
       - checks that all skills in module-help.csv exist in manifest
       - checks that all manifest skills exist in repository
       - validates semantic versioning (major.minor.patch)
       - reports detailed findings or pass status
- [ ] VM Validation Results
     - Given: VM validation completes
     - When: results are returned
     - Then:
       - Pass: all checks pass, module is approved for distribution
       - Fail with findings: issues listed with remediation guidance
- [ ] VM Issue Resolution
     - Given: VM finds structural issues
     - When: issues are reported
     - Then:
       - release is blocked until resolved
       - remediation guidance is specific and actionable
       - user can re-run VM after fixes
- [ ] Integration with Release
     - Given: module is ready to release
     - When: CM and VM gates pass
     - Then: module can proceed to distribution

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 11.1](../stories/11.1-implement-module-identity-and-configuration-schema.md), [Story 11.2](../stories/11.2-implement-module-help-registration.md), [Story 11.3](../stories/11.3-implement-marketplace-manifest.md).
- Definition of done from the source plan:
  - CM command is implemented and regenerates all files
  - VM command is implemented and validates all checks
  - Pass/fail results are reported clearly
  - Integration with release process is established

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

