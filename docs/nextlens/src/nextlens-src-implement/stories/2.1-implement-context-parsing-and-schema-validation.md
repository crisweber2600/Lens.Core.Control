---
feature: nextlens-src-implement
story_id: "2.1"
doc_type: story
status: ready
title: "Implement Context Parsing and Schema Validation"
epic: "EP2: Context Sufficiency Gate"
priority: High
complexity: Medium
depends_on: ["1.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 2.1: Implement Context Parsing and Schema Validation

## Context

As a **NextLens system**, I want to **parse and validate incoming top-down context against the lens.topdown-context.v1 schema**, so that **I can detect missing or malformed input early**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 1.1](../stories/1.1-implement-command-entry-point-and-argument-parser.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Context Parsing and Schema Validation`.
2. Implement the acceptance slice `Schema Validation`.
3. Implement the acceptance slice `Schema Version Check`.
4. Implement the acceptance slice `Malformed Input`.
5. Implement the acceptance slice `Nested Schema Validation`.
6. Implement the acceptance slice `Context Loading`.
7. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
8. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Schema Validation
     - Given: top-down context is provided in YAML format
     - When: context is parsed
     - Then: required root fields are present: schemaVersion, system, discoveryEpoch, roles, outcomes, journeys, candidateFeatures
- [ ] Schema Version Check
     - Given: schemaVersion is not `lens.topdown-context.v1`
     - When: parsing completes
     - Then: warning is logged, parsing continues with version-mismatch flag
- [ ] Malformed Input
     - Given: malformed YAML or missing required fields
     - When: parsing is attempted
     - Then: detailed error message identifies missing field, parsing stops
- [ ] Nested Schema Validation
     - Given: nested objects (system, roles, outcomes) are parsed
     - When: validation runs
     - Then: required nested fields are checked (system.id, role.id, outcome.id, etc.)
- [ ] Context Loading
     - Given: context file path is specified
     - When: loading runs
     - Then: file is read, parsed as YAML, and validated

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 1.1](../stories/1.1-implement-command-entry-point-and-argument-parser.md).
- Definition of done from the source plan:
  - Schema validation is implemented against lens.topdown-context.v1
  - Malformed input produces clear error messages
  - Version mismatch is warned but not fatal
  - Nested fields are validated
  - Unit tests cover valid and invalid schemas

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

