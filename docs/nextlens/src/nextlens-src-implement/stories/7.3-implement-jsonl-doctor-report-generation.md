---
feature: nextlens-src-implement
story_id: "7.3"
doc_type: story
status: done
title: "Implement JSONL Doctor Report Generation"
epic: "EP7: Doctor Checks & Validation"
priority: High
complexity: Medium
depends_on: ["7.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 7.3: Implement JSONL Doctor Report Generation

## Context

As a **NextLens system**, I want to **emit doctor report as JSONL with one check result per line**, so that **operators can parse results with shell tools and CI systems**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 7.2](../stories/7.2-implement-core-doctor-checks.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement JSONL Doctor Report Generation`.
2. Implement the acceptance slice `Report File Creation`.
3. Implement the acceptance slice `Report Line Format`.
4. Implement the acceptance slice `Result Fields`.
5. Implement the acceptance slice `Summary Line`.
6. Implement the acceptance slice `JSONL Compliance`.
7. Implement the acceptance slice `Report Readability`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Report File Creation
     - Given: all doctor checks complete
     - When: report generation runs
     - Then: report is written to {docs_path}/.nextlens/doctor-{run_id}.jsonl
- [ ] Report Line Format
     - Given: each check completes
     - When: result is written to report
     - Then: format is valid JSON on single line:
       ```json
       {"timestamp": "2026-05-14T12:34:56Z", "check_id": "schema-validity", "status": "pass", "severity": "blocking", "message": "All landscape files valid", "references": []}
       ```
- [ ] Result Fields
     - Given: check result is written
     - When: fields are populated
     - Then: fields include:
       - timestamp (ISO 8601)
       - check_id (stable identifier)
       - status (pass/warning/fail)
       - severity (blocking/advisory/informational)
       - message (human-readable)
       - references (affected entity IDs or paths)
       - remediation (if applicable)
- [ ] Summary Line
     - Given: all checks complete
     - When: summary line is written
     - Then: final line includes:
       - checks_run: count
       - passed: count
       - blocked: count
       - advisory: count
       - overall_status: pass/advisory/blocked
- [ ] JSONL Compliance
     - Given: report is generated
     - When: format is validated
     - Then:
       - each line is valid JSON
       - newline-delimited (one result per line)
       - readable by standard JSONL parsers
- [ ] Report Readability
     - Given: report is generated
     - When: it is parsed by shell tools
     - Then: it can be processed with `jq`, `grep`, `awk` without issues

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 7.2](../stories/7.2-implement-core-doctor-checks.md).
- Definition of done from the source plan:
  - JSONL report generation is implemented
  - Report format is correct and readable
  - All required fields are present
  - Integration test verifies report generation and parsing

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

