---
feature: nextlens-src-implement
story_id: "3.3"
doc_type: story
status: ready
title: "Implement State Reconstruction and Query"
epic: "EP3: Landscape State Management"
priority: High
complexity: Medium
depends_on: ["3.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 3.3: Implement State Reconstruction and Query

## Context

As a **NextLens system**, I want to **reconstruct landscape state deterministically from persisted files**, so that **replay runs produce identical results**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 3.2](../stories/3.2-implement-landscape-state-persistence.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement State Reconstruction and Query`.
2. Implement the acceptance slice `Entity Loading`.
3. Implement the acceptance slice `Relationship Resolution`.
4. Implement the acceptance slice `Query by ID`.
5. Implement the acceptance slice `Query by Type`.
6. Implement the acceptance slice `Relationship Queries`.
7. Implement the acceptance slice `Validation on Reconstruction`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Entity Loading
     - Given: existing landscape files at {docs_path}/landscape/
     - When: state reconstruction runs
     - Then: all entities are loaded in dependency order (systems → roles/outcomes → journeys/operating loops → capabilities)
- [ ] Relationship Resolution
     - Given: entity relationships are recorded in files
     - When: state is reconstructed
     - Then: all parent/child and cross-entity references are validated and resolved
- [ ] Query by ID
     - Given: state is fully reconstructed
     - When: query runs for a specific entity by stable ID
     - Then: the entity and all its relationships are returned without additional I/O
- [ ] Query by Type
     - Given: state is reconstructed
     - When: query runs for all entities of a type (e.g., all outcomes)
     - Then: all entities of that type are returned in deterministic order
- [ ] Relationship Queries
     - Given: state is reconstructed
     - When: query runs for entities related to a specific entity (e.g., "all journeys for outcome X")
     - Then: related entities are returned with edge metadata
- [ ] Validation on Reconstruction
     - Given: state is reconstructed
     - When: validation runs
     - Then: missing or broken references are reported as warnings; state is still usable

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 3.2](../stories/3.2-implement-landscape-state-persistence.md).
- Definition of done from the source plan:
  - Entity loading in correct dependency order
  - Relationship resolution is complete
  - Query API is performant (no unnecessary I/O)
  - Unit tests cover all query types
  - Integration test verifies reconstruction matches original state

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

