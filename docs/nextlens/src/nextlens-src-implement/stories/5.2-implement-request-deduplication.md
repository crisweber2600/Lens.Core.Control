---
feature: nextlens-src-implement
story_id: "5.2"
doc_type: story
status: ready
title: "Implement Request Deduplication"
epic: "EP5: Idempotent Writes & Replay Safety"
priority: Critical
complexity: Medium
depends_on: ["5.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 5.2: Implement Request Deduplication

## Context

As a **NextLens system**, I want to **detect and deduplicate duplicate requests using idempotency tokens**, so that **reruns with the same token never create side effects**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Request Deduplication`.
2. Implement the acceptance slice `Token Lookup`.
3. Implement the acceptance slice `New Request Path`.
4. Implement the acceptance slice `Completed Request Path`.
5. Implement the acceptance slice `Pending Request Path`.
6. Implement the acceptance slice `Failed Request Path`.
7. Implement the acceptance slice `Deduplication Performance`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Token Lookup
     - Given: a request arrives with idempotency token
     - When: deduplication check runs
     - Then: system checks if token exists in store at {docs_path}/.idempotency/tokens/{token-value}.yaml
- [ ] New Request Path
     - Given: token does not exist
     - When: check completes
     - Then: operation is marked as "new" and proceeds to execution
- [ ] Completed Request Path
     - Given: token exists with status=completed
     - When: check completes
     - Then: operation is skipped; original result is returned (replay)
- [ ] Pending Request Path
     - Given: token exists with status=pending
     - When: check completes
     - Then: system waits for completion (with timeout) or returns error with retry-after suggestion
- [ ] Failed Request Path
     - Given: token exists with status=failed
     - When: check completes
     - Then: system returns error and suggests contact support or manual recovery
- [ ] Deduplication Performance
     - Given: token lookup runs
     - When: performance is measured
     - Then: lookup completes in < 100ms (file I/O bound)

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md).
- Definition of done from the source plan:
  - Token lookup is implemented
  - All four request paths are tested
  - Deduplication prevents duplicate side effects
  - Integration test verifies replay semantics

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

