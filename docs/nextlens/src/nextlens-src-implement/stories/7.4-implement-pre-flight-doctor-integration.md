---
feature: nextlens-src-implement
story_id: "7.4"
doc_type: story
status: done
title: "Implement Pre-Flight Doctor Integration"
epic: "EP7: Doctor Checks & Validation"
priority: Critical
complexity: Medium
depends_on: ["1.3", "7.3"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 7.4: Implement Pre-Flight Doctor Integration

## Context

As a **NextLens system**, I want to **run all doctor checks before packet emission and block emission if blocking findings exist**, so that **invalid packets never reach BMAD**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 1.3](../stories/1.3-implement-status-output-formatting-and-progress-display.md), [Story 7.3](../stories/7.3-implement-jsonl-doctor-report-generation.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Pre-Flight Doctor Integration`.
2. Implement the acceptance slice `Pre-Flight Stage Trigger`.
3. Implement the acceptance slice `Blocking Findings Detection`.
4. Implement the acceptance slice `Advisory Findings Display`.
5. Implement the acceptance slice `Operator Confirmation on Advisory`.
6. Implement the acceptance slice `Operator Cancellation`.
7. Implement the acceptance slice `Informational Findings Logging`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Pre-Flight Stage Trigger
     - Given: packet emission is about to start
     - When: pre-flight stage runs
     - Then: all doctor checks execute automatically (not optional)
- [ ] Blocking Findings Detection
     - Given: doctor finds blocking-severity findings
     - When: pre-flight completes
     - Then:
       - packet emission is blocked
       - status is fail
       - findings are displayed with remediation hints
       - operator must fix issues or cancel
- [ ] Advisory Findings Display
     - Given: doctor finds advisory-severity findings
     - When: pre-flight completes
     - Then:
       - packet emission can proceed
       - operator is prompted for explicit confirmation ("Proceed with advisory findings?" [Y/n])
- [ ] Operator Confirmation on Advisory
     - Given: advisory findings exist
     - When: operator is prompted
     - Then:
       - If Y: proceed to emission
       - If n: stop execution, no writes, packet not emitted
- [ ] Operator Cancellation
     - Given: operator declines to proceed with advisory findings
     - When: user selects no
     - Then: execution stops cleanly, no writes occur, packet is not emitted
- [ ] Informational Findings Logging
     - Given: informational-severity findings exist
     - When: pre-flight completes
     - Then: findings are logged but do not block or prompt for confirmation

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 1.3](../stories/1.3-implement-status-output-formatting-and-progress-display.md), [Story 7.3](../stories/7.3-implement-jsonl-doctor-report-generation.md).
- Definition of done from the source plan:
  - Pre-flight is triggered automatically before emission
  - Blocking findings prevent emission
  - Advisory findings prompt for confirmation
  - Operator can confirm or decline
  - Integration test verifies pre-flight flow

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

