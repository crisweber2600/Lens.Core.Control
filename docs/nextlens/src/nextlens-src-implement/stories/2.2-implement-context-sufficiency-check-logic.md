---
feature: nextlens-src-implement
story_id: "2.2"
doc_type: story
status: ready
title: "Implement Context Sufficiency Check Logic"
epic: "EP2: Context Sufficiency Gate"
priority: Critical
complexity: High
depends_on: ["2.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 2.2: Implement Context Sufficiency Check Logic

## Context

As a **NextLens system**, I want to **evaluate context completeness against the seven mandatory gates**, so that **I prevent silent failures due to incomplete discovery**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 2.1](../stories/2.1-implement-context-parsing-and-schema-validation.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Context Sufficiency Check Logic`.
2. Implement the acceptance slice `Gate 1: System Thesis`.
3. Implement the acceptance slice `Gate 2: Role Coverage`.
4. Implement the acceptance slice `Gate 3: Outcome Coverage`.
5. Implement the acceptance slice `Gate 4: Journey/Hypothesis Coverage`.
6. Implement the acceptance slice `Gate 5: Candidate Traceability`.
7. Implement the acceptance slice `Gate 6: Risks and Open Questions`.
8. Implement the acceptance slice `Gate 7: BMAD Consumer Context`.
9. Implement the acceptance slice `Status Determination`.
10. Implement the acceptance slice `Missing Required Reporting`.
11. Implement the acceptance slice `Warnings Reporting`.
12. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
13. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Gate 1: System Thesis
     - Given: context is checked
     - When: system.thesis is evaluated
     - Then: pass if thesis is present and non-empty (>0 chars); fail otherwise
- [ ] Gate 2: Role Coverage
     - Given: context is checked
     - When: roles array is evaluated
     - Then: pass if at least one role is present; fail otherwise
- [ ] Gate 3: Outcome Coverage
     - Given: context is checked
     - When: outcomes array is evaluated
     - Then: pass if at least one outcome is present; fail otherwise
- [ ] Gate 4: Journey/Hypothesis Coverage
     - Given: context is checked
     - When: journeys array or journey hypotheses are evaluated
     - Then: pass if at least one journey or hypothesis is present; fail otherwise
- [ ] Gate 5: Candidate Traceability
     - Given: context is checked
     - When: candidateFeatures are evaluated
     - Then: pass if each candidate traces to at least one journey or outcome; fail otherwise
- [ ] Gate 6: Risks and Open Questions
     - Given: context is checked
     - When: risks and openQuestions arrays are evaluated
     - Then: pass if both present (may be empty); advisory if sparse (< 3 items each)
- [ ] Gate 7: BMAD Consumer Context
     - Given: context is checked
     - When: bmadConsumerContext field is evaluated
     - Then: pass if provided; advisory if missing
- [ ] Status Determination
     - Given: all gates are evaluated
     - When: status is determined
     - Then:
       - `ready` if all gates pass
       - `ready_with_warnings` if gates pass but advisories exist
       - `blocked` if any gate fails
- [ ] Missing Required Reporting
     - Given: one or more gates fail
     - When: status is blocked
     - Then: missingRequired array lists all failed gates
- [ ] Warnings Reporting
      - Given: gates pass but advisories exist
      - When: status is ready_with_warnings
      - Then: warnings array lists conditions

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 2.1](../stories/2.1-implement-context-parsing-and-schema-validation.md).
- Definition of done from the source plan:
  - All seven gates are implemented and tested
  - Status determination logic is correct
  - Missing required and warnings arrays are populated accurately
  - Unit tests cover pass, ready_with_warnings, and blocked scenarios

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

