---
feature: nextlens-src-implement
story_id: "4.1"
doc_type: story
status: ready
title: "Implement Deterministic Scoring Algorithm"
epic: "EP4: Feature Ranking & Selection"
priority: Critical
complexity: High
depends_on: ["2.3", "3.3"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 4.1: Implement Deterministic Scoring Algorithm

## Context

As a **NextLens system**, I want to **score candidate Features using nine fixed factors in deterministic order**, so that **ranking is reproducible and explainable**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 2.3](../stories/2.3-implement-context-sufficiency-report-output.md), [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Deterministic Scoring Algorithm`.
2. Implement the acceptance slice `Outcome Alignment Score`.
3. Implement the acceptance slice `Journey Criticality Score`.
4. Implement the acceptance slice `Role Value Score`.
5. Implement the acceptance slice `Risk Reduction Score`.
6. Implement the acceptance slice `Dependency Readiness Score`.
7. Implement the acceptance slice `Implementation Boundedness Score`.
8. Implement the acceptance slice `BMAD Readiness Score`.
9. Implement the acceptance slice `Evidence Clarity Score`.
10. Implement the acceptance slice `Open-Question Severity Score`.
11. Implement the acceptance slice `Composite Score`.
12. Implement the acceptance slice `Scoring Determinism`.
13. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
14. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Outcome Alignment Score
     - Given: candidate Feature with context
     - When: scoring runs
     - Then: score = (count of covered outcomes × weight by criticality) / total outcomes
     - Range: 0-100
- [ ] Journey Criticality Score
     - Given: candidate Feature with context
     - When: scoring runs
     - Then: score = (count of journeys covered × weight by role dependency) / total journeys
     - Range: 0-100
- [ ] Role Value Score
     - Given: candidate Feature with context
     - When: scoring runs
     - Then: score = (count of roles impacted × weight by stakeholder importance) / total roles
     - Range: 0-100
- [ ] Risk Reduction Score
     - Given: candidate Feature with context
     - When: scoring runs
     - Then: if total risks = 0, score defaults to 100; otherwise score = (count of mitigated risks × weight by severity) / total risks
     - Range: 0-100
- [ ] Dependency Readiness Score
     - Given: candidate Feature with dependencies
     - When: scoring runs
     - Then: score = (count of satisfied upstream dependencies) / total dependencies
     - Range: 0-100
- [ ] Implementation Boundedness Score
     - Given: candidate Feature with scope
     - When: scoring runs
     - Then: score = clarity penalty (0-100), higher if scope is clear and bounded, lower if spillage detected
- [ ] BMAD Readiness Score
     - Given: candidate Feature with BMAD inputs
     - When: scoring runs
     - Then: score = (count of available PRD, UX, architecture) / 3 × 100
     - Range: 0-100
- [ ] Evidence Clarity Score
     - Given: candidate Feature with rationale
     - When: scoring runs
     - Then: score = (traceability completeness: system → role → outcome → journey → feature) × 100
     - Range: 0-100
- [ ] Open-Question Severity Score
     - Given: candidate Feature with unresolved questions
     - When: scoring runs
     - Then: score = 100 - (count × severity weight) clamped to [0, 100]
- [ ] Composite Score
      - Given: all nine factor scores are calculated
      - When: composite score is computed
      - Then: composite = average of all nine scores (equal weight)
      - Range: 0-100
- [ ] Scoring Determinism
      - Given: identical inputs and state
      - When: scoring runs twice
      - Then: composite scores and ranking order are identical

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 2.3](../stories/2.3-implement-context-sufficiency-report-output.md), [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md).
- Definition of done from the source plan:
  - All nine scoring factors are implemented
  - Composite score calculation is deterministic
  - Unit tests verify each factor calculation
  - Integration test verifies reproducibility with identical inputs

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

