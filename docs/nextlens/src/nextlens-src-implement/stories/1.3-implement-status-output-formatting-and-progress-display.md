---
feature: nextlens-src-implement
story_id: "1.3"
doc_type: story
status: ready
title: "Implement Status Output Formatting and Progress Display"
epic: "EP1: Command Spine & Interactive Entry Point"
priority: High
complexity: Medium
depends_on: ["1.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 1.3: Implement Status Output Formatting and Progress Display

## Context

As a **NextLens operator**, I want the system to **display clear, scannable status messages at each stage without reliance on color or animation**, so that **I can follow progress in CI logs and terminal environments**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 1.2](../stories/1.2-implement-stage-pipeline-orchestration.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Status Output Formatting and Progress Display`.
2. Implement the acceptance slice `Status Line Format`.
3. Implement the acceptance slice `Warning Display`.
4. Implement the acceptance slice `Failure Display`.
5. Implement the acceptance slice `No Color Dependency`.
6. Implement the acceptance slice `Progress Tracking`.
7. Implement the acceptance slice `Line Wrapping`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Status Line Format
     - Given: each stage completes
     - When: status line is printed
     - Then: format is `[stage:stageName] status=pass|warning|fail; message; next_action=...`
- [ ] Warning Display
     - Given: warning-level findings exist
     - When: status is printed
     - Then: warnings are listed with remediation hints, not hidden by formatting
- [ ] Failure Display
     - Given: failure occurs before confirmation
     - When: status is printed
     - Then: rollback action is shown, diagnostic context is printed, next valid action is specified
- [ ] No Color Dependency
     - Given: output is displayed
     - When: terminal color is disabled
     - Then: all messages remain readable and understandable
- [ ] Progress Tracking
     - Given: multi-stage execution runs
     - When: each stage completes
     - Then: operator can see progress (e.g., "Stage 3 of 9 complete")
- [ ] Line Wrapping
     - Given: terminal width is narrow (80 columns)
     - When: output is rendered
     - Then: text wraps appropriately; no information is lost

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 1.2](../stories/1.2-implement-stage-pipeline-orchestration.md).
- Definition of done from the source plan:
  - All status messages follow consistent format
  - Messages are color-independent and readable
  - Warning and failure messages include remediation hints
  - Progress indicator shows current stage number
  - Output works in CI logs (GitHub Actions, etc.)
  - Manual QA on various terminal widths

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

