---
feature: nextlens-src-implement
story_id: "8.3"
doc_type: story
status: ready
title: "Implement Confirmation Gate"
epic: "EP8: Feature Packet Schema & Emission"
priority: High
complexity: Low
depends_on: ["1.3", "7.4", "8.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 8.3: Implement Confirmation Gate

## Context

As a **NextLens operator**, I want to **review the selected Feature and explicitly confirm before packet emission**, so that **I have final control and can cancel if needed**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 1.3](../stories/1.3-implement-status-output-formatting-and-progress-display.md), [Story 7.4](../stories/7.4-implement-pre-flight-doctor-integration.md), [Story 8.2](../stories/8.2-implement-packet-composition-logic.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Confirmation Gate`.
2. Implement the acceptance slice `Confirmation Display`.
3. Implement the acceptance slice `Confirm Action`.
4. Implement the acceptance slice `Cancel Action`.
5. Implement the acceptance slice `Input Validation`.
6. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
7. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Confirmation Display
     - Given: Feature is ranked and confirmed, doctor validation passed, packet is composed
     - When: confirmation gate stage runs
     - Then: display format is:
       ```
       [stage:final-confirmation]
       About to emit Feature packet:
       - Packet ID: [packetId]
       - Feature: [name]
       - Goal: [goal]
       - Scope: [includedScope summary]
       - Out of Scope: [explicitOutOfScope summary]
       - Evidence: [traceability summary]
       - Doctor Status: [pass|advisory|blocked]
       Emit packet? [Y/n]
       ```
- [ ] Confirm Action
     - Given: operator confirms (Y)
     - When: confirmation is recorded
     - Then: proceed to emission stage, record confirmation in evidence bundle with timestamp
- [ ] Cancel Action
     - Given: operator declines (n)
     - When: cancellation is recorded
     - Then:
       - stop execution
       - no writes occur
       - no packet emitted
       - preserve diagnostic context for resume
- [ ] Input Validation
     - Given: user provides invalid input (not Y or n)
     - When: input is processed
     - Then: prompt is re-displayed: "Please enter Y or n:"

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 1.3](../stories/1.3-implement-status-output-formatting-and-progress-display.md), [Story 7.4](../stories/7.4-implement-pre-flight-doctor-integration.md), [Story 8.2](../stories/8.2-implement-packet-composition-logic.md).
- Definition of done from the source plan:
  - Confirmation prompt is displayed correctly
  - Confirm action proceeds to emission
  - Cancel action stops execution
  - Input validation handles invalid entries

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

