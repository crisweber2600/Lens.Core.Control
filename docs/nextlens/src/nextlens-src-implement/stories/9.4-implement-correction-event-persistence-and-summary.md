---
feature: nextlens-src-implement
story_id: "9.4"
doc_type: story
status: done
title: "Implement Correction Event Persistence and Summary"
epic: "EP9: Salmon Correction Routing"
priority: High
complexity: Medium
depends_on: ["9.3"]
updated_at: 2026-05-15T01:13:43Z
---

# Story 9.4: Implement Correction Event Persistence and Summary

## Context

As a **NextLens system**, I want to **persist all Salmon events with audit trail**, so that **corrections are traceable and reproducible**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 9.3](../stories/9.3-implement-impact-classification-and-routing.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Correction Event Persistence and Summary`.
2. Implement the acceptance slice `Event Persistence`.
3. Implement the acceptance slice `Directory Structure`.
4. Implement the acceptance slice `Event Record Format`.
5. Implement the acceptance slice `Summary Generation`.
6. Implement the acceptance slice `Atomic Persistence`.
7. Implement the acceptance slice `Persistence Logging`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Event Persistence
     - Given: correction event is routed
     - When: persistence runs
     - Then: event YAML is written atomically to {docs_path}/.nextlens/salmon/{packetId}/{dedupFingerprint}.yaml
- [ ] Directory Structure
     - Given: event persistence runs
     - When: directory setup occurs
     - Then: directories are created:
       ```
       {docs_path}/.nextlens/salmon/
         {packetId}/
           [fingerprint1].yaml
           [fingerprint2].yaml
           ...
       ```
- [ ] Event Record Format
     - Given: event is persisted
     - When: format is determined
     - Then: YAML includes all event fields plus:
       - _metadata: {persistedAt, persistedBy, fileChecksum}
- [ ] Summary Generation
     - Given: one or more events are created per run
     - When: run completes
     - Then: summary is written to {docs_path}/.nextlens/salmon-summary-{packetId}.yaml with:
       ```yaml
       run_id: [UUID]
       packet_id: [packetId]
       events_created: [count]
       events_merged: [count]
       duplicates_ignored: [count]
       impact_distribution:
         local_feature_note: [count]
         feature_scope_change: [count]
         journey_assumption_change: [count]
         outcome_reframe: [count]
         role_or_stakeholder_change: [count]
         operating_loop_change: [count]
         capability_or_landscape_update: [count]
         bmad_correct_course_required: [count]
       routing_results:
         created: [count]
         merged: [count]
         ignored: [count]
       ```
- [ ] Atomic Persistence
     - Given: event is being persisted
     - When: persistence runs
     - Then: writes are atomic (temp file → rename) to prevent partial writes
- [ ] Persistence Logging
     - Given: events are persisted
     - When: persistence completes
     - Then: evidence bundle records:
       - event count
       - routing decisions
       - any errors or warnings

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 9.3](../stories/9.3-implement-impact-classification-and-routing.md).
- Definition of done from the source plan:
  - Event persistence is atomic
  - Summary is generated accurately
  - Directory structure is created correctly
  - Integration test verifies persistence and summary

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

