---
feature: nextlens-src-implement
story_id: "8.2"
doc_type: story
status: done
title: "Implement Packet Composition Logic"
epic: "EP8: Feature Packet Schema & Emission"
priority: High
complexity: High
depends_on: ["3.3", "4.1", "4.3", "6.2", "7.2"]
updated_at: 2026-05-15T01:00:22Z
---

# Story 8.2: Implement Packet Composition Logic

## Context

As a **NextLens system**, I want to **assemble Feature packet from ranking results, landscape state, and context**, so that **packet contains all required information for BMAD downstream**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md), [Story 4.1](../stories/4.1-implement-deterministic-scoring-algorithm.md), [Story 4.3](../stories/4.3-implement-candidate-presentation-and-selection.md), [Story 6.2](../stories/6.2-implement-eager-graph-rebuild.md), [Story 7.2](../stories/7.2-implement-core-doctor-checks.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Packet Composition Logic`.
2. Implement the acceptance slice `Packet Assembly`.
3. Implement the acceptance slice `Selected Feature Population`.
4. Implement the acceptance slice `Trace Population`.
5. Implement the acceptance slice `Selection Rationale Population`.
6. Implement the acceptance slice `Source Context References`.
7. Implement the acceptance slice `State References`.
8. Implement the acceptance slice `Doctor Summary Population`.
9. Implement the acceptance slice `BMAD Hints Population`.
10. Implement the acceptance slice `Timestamps`.
11. Implement the acceptance slice `Packet Validation`.
12. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
13. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Packet Assembly
     - Given: Feature is selected and confirmed
     - When: packet composition runs
     - Then: system assembles:
       - schemaVersion = "nextlens.feature-packet.v1"
       - packetId from unique generator (UUID v4)
       - featureId from selected candidate
       - sourceMode = "top_down"
- [ ] Selected Feature Population
     - Given: Feature is selected
     - When: selectedFeature is populated
     - Then:
       - name from Feature definition
       - goal from Feature definition
       - includedScope from Feature definition
       - explicitOutOfScope populated with: adjacent journeys, future Features, platform architecture, unrelated outcomes
- [ ] Trace Population
     - Given: context is available
     - When: trace is populated
     - Then:
       - systemId from discovered system
       - discoveryEpochId from discovery context
       - roleIds from all impacted roles
       - outcomeIds from all covered outcomes
       - journeyIds from all involved journeys
       - operatingLoopIds from affected operating loops
       - relationshipRefs to preserve lineage
- [ ] Selection Rationale Population
     - Given: ranking results are available
     - When: selectionRationale is populated
     - Then:
       - score from composite score
       - tieBreakEvidence from tie-break sequence (if applied)
       - whyThisFeature textual explanation
       - whyNow timing rationale
       - rejectedAlternates list of rank 2, 3 candidates and scores
- [ ] Source Context References
     - Given: input documents are available
     - When: sourceContextRefs is populated
     - Then: array includes paths: product-brief.md, prd.md, ux-design.md, architecture.md, research.md, brainstorm.md
- [ ] State References
     - Given: authoritative state and derived graph are available
     - When: references are populated
     - Then:
       - authoritativeStateRef = path to landscape directory
       - derivedGraphRef = path to derived graph JSON
- [ ] Doctor Summary Population
     - Given: doctor checks complete
     - When: doctorSummary is populated
     - Then:
       - status from doctor overall_status
       - blocking_count
       - advisory_count
       - informational_count
- [ ] BMAD Hints Population
     - Given: context is available
     - When: bmadConsumerHints is populated
     - Then: hints include:
       - warning about scope containment
       - prdInput: PRD goal and key requirements
       - uxInput: UX patterns and key flows
       - architectureInput: architecture decisions affecting Feature
       - epicStoryInput: estimated epic/story breakdown
       - readinessInput: current readiness status
- [ ] Timestamps
     - Given: packet is composed
     - When: createdAt is set
     - Then: timestamp is ISO 8601 format with UTC timezone
- [ ] Packet Validation
      - Given: packet composition completes
      - When: schema validation runs
      - Then: all required fields are present and valid (Story 8.1)

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md), [Story 4.1](../stories/4.1-implement-deterministic-scoring-algorithm.md), [Story 4.3](../stories/4.3-implement-candidate-presentation-and-selection.md), [Story 6.2](../stories/6.2-implement-eager-graph-rebuild.md), [Story 7.2](../stories/7.2-implement-core-doctor-checks.md).
- Definition of done from the source plan:
  - Packet assembly logic is implemented
  - All required fields are populated
  - References are accurate and resolvable
  - Schema validation passes
  - Integration test verifies full composition

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

