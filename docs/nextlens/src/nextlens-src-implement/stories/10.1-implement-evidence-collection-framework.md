---
feature: nextlens-src-implement
story_id: "10.1"
doc_type: story
status: done
title: "Implement Evidence Collection Framework"
epic: "EP10: Evidence Bundle & Traceability"
priority: High
complexity: Medium
depends_on: []
updated_at: 2026-05-15T01:17:56Z
---

# Story 10.1: Implement Evidence Collection Framework

## Context

As a **NextLens system**, I want to **collect evidence from all stages in a structured, traceable format**, so that **operators and reviewers can reconstruct the run**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
This story has no upstream story dependencies.

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Evidence Collection Framework`.
2. Implement the acceptance slice `Evidence Collector Initialization`.
3. Implement the acceptance slice `Per-Stage Evidence Capture`.
4. Implement the acceptance slice `Evidence Collection Points`.
5. Implement the acceptance slice `Evidence Aggregation`.
6. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
7. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Evidence Collector Initialization
     - Given: command starts
     - When: evidence collector is initialized
     - Then: collector creates run record with:
       - run_id: unique UUID
       - started_at: ISO 8601 timestamp
       - stage_records: [] (empty array for stage results)
- [ ] Per-Stage Evidence Capture
     - Given: each stage completes
     - When: stage result is available
     - Then: evidence collector records:
       - stage_name (context-intake, context-sufficiency, feature-ranking, etc.)
       - timestamp (stage start and end)
       - status (pass/warning/fail)
       - input summary (count of items processed, key values)
       - output summary (decisions made, confirmations given, blocks encountered)
       - duration (elapsed time in seconds)
       - any warnings or diagnostic output
- [ ] Evidence Collection Points
     - Given: all stages execute
     - When: evidence is collected
     - Then: collection points include:
       - command arguments and config
       - context intake and parsing
       - context sufficiency check result
       - landscape state reconstruction
       - feature ranking and tie-break application
       - operator confirmations
       - doctor validation results
       - packet emission result
       - salmon routing results
       - any errors or exceptions
- [ ] Evidence Aggregation
     - Given: all stages complete
     - When: evidence collection finishes
     - Then: evidence manifest is available for bundle assembly

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Definition of done from the source plan:
  - Evidence collector is initialized at command start
  - All stages record evidence
  - Evidence format is structured and queryable
  - Unit tests verify evidence collection

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

