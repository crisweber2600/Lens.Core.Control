---
feature: nextlens-src-implement
story_id: "4.2"
doc_type: story
status: ready
title: "Implement Tie-Break Sequence"
epic: "EP4: Feature Ranking & Selection"
priority: High
complexity: Medium
depends_on: ["4.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 4.2: Implement Tie-Break Sequence

## Context

As a **NextLens system**, I want to **apply deterministic tie-break logic when candidates have equal composite scores**, so that **tie-break is always the same given identical inputs**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 4.1](../stories/4.1-implement-deterministic-scoring-algorithm.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Tie-Break Sequence`.
2. Implement the acceptance slice `Tier 1 Tie-Break: Outcome Alignment`.
3. Implement the acceptance slice `Tier 2 Tie-Break: Journey Criticality`.
4. Implement the acceptance slice `Tier 3 Tie-Break: Unresolved Blockers`.
5. Implement the acceptance slice `Tier 4 Tie-Break: Evidence Clarity`.
6. Implement the acceptance slice `Tier 5 Tie-Break: Candidate Timestamp`.
7. Implement the acceptance slice `Tier 6 Tie-Break: Lexical Order`.
8. Implement the acceptance slice `Full Tie-Break Determinism`.
9. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
10. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Tier 1 Tie-Break: Outcome Alignment
     - Given: two candidates with equal composite score
     - When: tie-break runs
     - Then: candidate with highest outcome alignment score ranks first
- [ ] Tier 2 Tie-Break: Journey Criticality
     - Given: outcome alignment scores are also equal
     - When: tier 2 runs
     - Then: candidate with highest journey criticality score ranks first
- [ ] Tier 3 Tie-Break: Unresolved Blockers
     - Given: tier 2 scores are equal
     - When: tier 3 runs
     - Then: candidate with fewest unresolved blockers ranks first
- [ ] Tier 4 Tie-Break: Evidence Clarity
     - Given: tier 3 scores are equal
     - When: tier 4 runs
     - Then: candidate with highest evidence clarity score ranks first
- [ ] Tier 5 Tie-Break: Candidate Timestamp
     - Given: tier 4 scores are equal
     - When: tier 5 runs
     - Then: candidate with earliest stable candidate timestamp (first discovered) ranks first
- [ ] Tier 6 Tie-Break: Lexical Order
     - Given: tier 5 timestamps are equal
     - When: tier 6 runs
     - Then: lexical ordering of Feature ID is applied (A before Z)
- [ ] Full Tie-Break Determinism
     - Given: identical inputs
     - When: tie-break sequence runs twice
     - Then: results are identical

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 4.1](../stories/4.1-implement-deterministic-scoring-algorithm.md).
- Definition of done from the source plan:
  - All six tie-break tiers are implemented
  - Tie-break sequence is deterministic
  - Unit tests verify each tier
  - Integration test verifies full tie-break sequence

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

