---
feature: nextlens-src-implement
story_id: "3.2"
doc_type: story
status: ready
title: "Implement Landscape State Persistence"
epic: "EP3: Landscape State Management"
priority: High
complexity: High
depends_on: ["3.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 3.2: Implement Landscape State Persistence

## Context

As a **NextLens system**, I want to **persist landscape state in the control repo at {docs_path}/landscape/**, so that **state is version-controlled, auditable, and shareable**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 3.1](../stories/3.1-implement-stable-id-generation-and-validation.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Landscape State Persistence`.
2. Implement the acceptance slice `Directory Structure`.
3. Implement the acceptance slice `Entity File Persistence`.
4. Implement the acceptance slice `Write Success Validation`.
5. Implement the acceptance slice `Write Failure Handling`.
6. Implement the acceptance slice `Atomic Writes`.
7. Implement the acceptance slice `Permission Enforcement`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Directory Structure
     - Given: landscape state is ready to write
     - When: directory initialization runs
     - Then: directory structure is created:
       ```
       {docs_path}/landscape/
         system/
         role/
         outcome/
         journey/
         operating_loop/
         capability/
         decision/
         risk/
       ```
- [ ] Entity File Persistence
     - Given: authoritative state is ready to write
     - When: write stage executes
     - Then: state is written to {docs_path}/landscape/{entity-type}/{stable-id}.yaml with:
       - entity identity (semantic ID, opaque ID, name)
       - state snapshot (content specific to entity type)
       - relationships (parent/child, cross-entity references)
       - metadata (createdAt, updatedAt, source, author)
- [ ] Write Success Validation
     - Given: write completes successfully
     - When: file system is checked
     - Then: file is readable, parseable, and matches schema
- [ ] Write Failure Handling
     - Given: write fails (permission, disk space, etc.)
     - When: failure occurs
     - Then: error is logged, transaction is rolled back, status is fail, packet emission is blocked
- [ ] Atomic Writes
     - Given: landscape state is being written
     - When: write is executed
     - Then: writes are atomic (temp file → rename) to prevent partial writes on failure
- [ ] Permission Enforcement
     - Given: landscape files are written
     - When: write completes
     - Then: file permissions are set to allow control repo members to read and write

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 3.1](../stories/3.1-implement-stable-id-generation-and-validation.md).
- Definition of done from the source plan:
  - Directory structure is created automatically
  - Entity files are written with correct schema
  - Atomic writes are implemented
  - Write failures are handled gracefully
  - Integration test verifies persistence and recovery

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

