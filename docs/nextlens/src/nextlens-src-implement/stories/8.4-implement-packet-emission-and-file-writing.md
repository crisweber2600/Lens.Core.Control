---
feature: nextlens-src-implement
story_id: "8.4"
doc_type: story
status: done
title: "Implement Packet Emission and File Writing"
epic: "EP8: Feature Packet Schema & Emission"
priority: High
complexity: Medium
depends_on: ["5.1", "8.3"]
updated_at: 2026-05-15T01:03:17Z
---

# Story 8.4: Implement Packet Emission and File Writing

## Context

As a **NextLens system**, I want to **emit Feature packet to {docs_path}/.nextlens/ with deterministic naming**, so that **BMAD can locate and consume the packet**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md), [Story 8.3](../stories/8.3-implement-confirmation-gate.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Packet Emission and File Writing`.
2. Implement the acceptance slice `Directory Creation`.
3. Implement the acceptance slice `File Naming`.
4. Implement the acceptance slice `File Writing`.
5. Implement the acceptance slice `Atomic Write`.
6. Implement the acceptance slice `Write Success`.
7. Implement the acceptance slice `Write Failure`.
8. Implement the acceptance slice `File Permissions`.
9. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
10. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Directory Creation
     - Given: packet emission is starting
     - When: directory setup runs
     - Then: directory {docs_path}/.nextlens/ is created if not present
- [ ] File Naming
     - Given: packet is ready to write
     - When: filename is determined
     - Then: filename is `packet-{packetId}.json` where packetId is UUID
- [ ] File Writing
     - Given: confirmation gate passes
     - When: emission stage runs
     - Then: packet is written to {docs_path}/.nextlens/packet-{packetId}.json with:
       - JSON formatting (indented, readable)
       - UTF-8 encoding
       - file permissions (readable by control repo members)
- [ ] Atomic Write
     - Given: packet is being written
     - When: write is executed
     - Then: write is atomic (temp file → rename) to prevent partial writes on failure
- [ ] Write Success
     - Given: packet is written
     - When: write completes
     - Then:
       - status is pass
       - packet path is printed: `Packet emitted to: {docs_path}/.nextlens/packet-{packetId}.json`
       - evidence bundle records write result with timestamp
- [ ] Write Failure
     - Given: write fails
     - When: failure occurs
     - Then:
       - status is fail
       - error message identifies failure reason
       - rollback guidance is printed (partial write cleanup, etc.)
       - no evidence of partial write remains
       - packet is not emitted
- [ ] File Permissions
     - Given: packet file is written
     - When: permissions are set
     - Then: permissions allow control repo members to read and write

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md), [Story 8.3](../stories/8.3-implement-confirmation-gate.md).
- Definition of done from the source plan:
  - Directory creation is implemented
  - File writing is atomic
  - Write success is confirmed
  - Write failure is handled gracefully
  - Integration test verifies file creation and content

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

