---
feature: nextlens-src-implement
story_id: "3.1"
doc_type: story
status: ready
title: "Implement Stable ID Generation and Validation"
epic: "EP3: Landscape State Management"
priority: High
complexity: Medium
depends_on: []
updated_at: 2026-05-14T22:22:48Z
---

# Story 3.1: Implement Stable ID Generation and Validation

## Context

As a **NextLens system**, I want to **generate deterministic stable IDs for all landscape entities** and **validate that IDs are unique and immutable**, so that **I can reliably reference and trace entities across multiple runs**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
This story has no upstream story dependencies.

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Stable ID Generation and Validation`.
2. Implement the acceptance slice `Semantic ID Generation`.
3. Implement the acceptance slice `Opaque ID Generation`.
4. Implement the acceptance slice `ID Immutability`.
5. Implement the acceptance slice `ID Uniqueness`.
6. Implement the acceptance slice `ID Validation`.
7. Implement the acceptance slice `ID Format`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Semantic ID Generation
     - Given: a new landscape entity (system, role, outcome, journey, operating loop) is created
     - When: ID generation runs
     - Then: a semantic ID is generated (human-visible, normalized from entity name, e.g., "role-system-architect" from "System Architect")
- [ ] Opaque ID Generation
     - Given: semantic ID is generated
     - When: opaque ID generation runs
     - Then: an immutable machine reference is generated (UUID or deterministic hash based on semantic ID)
- [ ] ID Immutability
     - Given: an entity with stable ID already exists
     - When: reconstruction runs
     - Then: the same ID is recovered and used for all subsequent operations
- [ ] ID Uniqueness
     - Given: two entities with identical normalized names
     - When: ID generation runs
     - Then: both receive unique IDs (suffix-based disambiguation applied, e.g., "role-1", "role-2")
- [ ] ID Validation
     - Given: ID immutability is validated
     - When: doctor check runs
     - Then: if ID is changed, warning is reported; evidence bundle records rename event
- [ ] ID Format
     - Given: semantic ID is generated
     - When: format is determined
     - Then: format is lowercase, hyphen-separated, max 50 chars

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Definition of done from the source plan:
  - Semantic ID generation is deterministic
  - Opaque IDs are unique and immutable
  - ID uniqueness collision handling is tested
  - ID validation detects changes
  - Unit tests cover all ID generation paths

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

