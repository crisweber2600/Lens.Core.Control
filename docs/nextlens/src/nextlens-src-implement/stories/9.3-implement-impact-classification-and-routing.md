---
feature: nextlens-src-implement
story_id: "9.3"
doc_type: story
status: done
title: "Implement Impact Classification and Routing"
epic: "EP9: Salmon Correction Routing"
priority: High
complexity: High
depends_on: ["9.1", "9.2"]
updated_at: 2026-05-15T01:11:40Z
---

# Story 9.3: Implement Impact Classification and Routing

## Context

As a **NextLens system**, I want to **classify corrections by impact level and route to appropriate targets**, so that **corrections affect only intended scope**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 9.1](../stories/9.1-implement-salmon-event-model.md), [Story 9.2](../stories/9.2-implement-fingerprint-based-deduplication.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Impact Classification and Routing`.
2. Implement the acceptance slice `Impact Classification`.
3. Implement the acceptance slice `local_feature_note: affects only Feature notes, no state change`.
4. Implement the acceptance slice `feature_scope_change: changes Feature includedScope or explicitOutOfScope`.
5. Implement the acceptance slice `journey_assumption_change: affects Journey steps or preconditions`.
6. Implement the acceptance slice `outcome_reframe: redefines or reprioritizes Outcome`.
7. Implement the acceptance slice `role_or_stakeholder_change: adds/removes/redefines Role`.
8. Implement the acceptance slice `operating_loop_change: affects Operating Loop cycles`.
9. Implement the acceptance slice `capability_or_landscape_update: adds/updates system Capabilities`.
10. Implement the acceptance slice `bmad_correct_course_required: requires BMAD to rescope or replan`.
11. Implement the acceptance slice `Routing Logic: local_feature_note`.
12. Implement the acceptance slice `Routing Logic: feature_scope_change`.
13. Implement the acceptance slice `Routing Logic: journey_assumption_change`.
14. Implement the acceptance slice `Routing Logic: outcome_reframe`.
15. Implement the acceptance slice `Routing Logic: role_or_stakeholder_change`.
16. Implement the acceptance slice `Routing Logic: operating_loop_change`.
17. Implement the acceptance slice `Routing Logic: capability_or_landscape_update`.
18. Implement the acceptance slice `Routing Logic: bmad_correct_course_required`.
19. Implement the acceptance slice `Routing Result`.
20. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
21. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Impact Classification
     - Given: correction event is created
     - When: impact classification runs
     - Then: system classifies into one of eight levels:
- [ ] local_feature_note: affects only Feature notes, no state change
- [ ] feature_scope_change: changes Feature includedScope or explicitOutOfScope
- [ ] journey_assumption_change: affects Journey steps or preconditions
- [ ] outcome_reframe: redefines or reprioritizes Outcome
- [ ] role_or_stakeholder_change: adds/removes/redefines Role
- [ ] operating_loop_change: affects Operating Loop cycles
- [ ] capability_or_landscape_update: adds/updates system Capabilities
- [ ] bmad_correct_course_required: requires BMAD to rescope or replan
- [ ] Routing Logic: local_feature_note
     - Given: impact level is local_feature_note
     - When: routing logic runs
     - Then: target is Feature notes file; event is appended as comment
- [ ] Routing Logic: feature_scope_change
     - Given: impact level is feature_scope_change
     - When: routing logic runs
     - Then: target is Feature definition file; includedScope or explicitOutOfScope is updated; landscape update recorded
- [ ] Routing Logic: journey_assumption_change
     - Given: impact level is journey_assumption_change
     - When: routing logic runs
     - Then: target is Journey file; journey definition updated; if BMAD is active, BMAD is notified
- [ ] Routing Logic: outcome_reframe
     - Given: impact level is outcome_reframe
     - When: routing logic runs
     - Then: target is Outcome file; outcome updated; all impacted Features and Journeys are notified
- [ ] Routing Logic: role_or_stakeholder_change
     - Given: impact level is role_or_stakeholder_change
     - When: routing logic runs
     - Then: target is Role file; role updated; all impacted Features, Journeys, Outcomes are notified
- [ ] Routing Logic: operating_loop_change
     - Given: impact level is operating_loop_change
     - When: routing logic runs
     - Then: target is OperatingLoop file; loop updated; impacted Journeys are notified
- [ ] Routing Logic: capability_or_landscape_update
     - Given: impact level is capability_or_landscape_update
     - When: routing logic runs
     - Then: target is Capability file and Landscape updates; new entity records created if needed
- [ ] Routing Logic: bmad_correct_course_required
     - Given: impact level is bmad_correct_course_required
     - When: routing logic runs
     - Then: target is Feature packet and BMAD correct-course record; correction-tracking record created
- [ ] Routing Result
      - Given: routing decision is made
      - When: event is persisted
      - Then: routingResult includes:
        - status: created|merged|duplicate_ignored
        - targetRef: path or entity ID where routed
        - timestamp: when routed

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 9.1](../stories/9.1-implement-salmon-event-model.md), [Story 9.2](../stories/9.2-implement-fingerprint-based-deduplication.md).
- Definition of done from the source plan:
  - All eight impact levels are classified correctly
  - Routing logic is implemented for each level
  - Routing results are recorded
  - Integration test verifies routing end-to-end

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

