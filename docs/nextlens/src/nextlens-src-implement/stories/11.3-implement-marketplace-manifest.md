---
feature: nextlens-src-implement
story_id: "11.3"
doc_type: story
status: ready
title: "Implement Marketplace Manifest"
epic: "EP11: BMAD Module Packaging & Registration"
priority: High
complexity: Medium
depends_on: ["11.1", "11.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 11.3: Implement Marketplace Manifest

## Context

As a **NextLens module designer**, I want to **create .claude-plugin/marketplace.json for installer and distribution compatibility**, so that **NextLens can be installed from package sources**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 11.1](../stories/11.1-implement-module-identity-and-configuration-schema.md), [Story 11.2](../stories/11.2-implement-module-help-registration.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Marketplace Manifest`.
2. Implement the acceptance slice `Manifest Directory`.
3. Implement the acceptance slice `Manifest Content`.
4. Implement the acceptance slice `Plugin Definitions`.
5. Implement the acceptance slice `Skills Path References`.
6. Implement the acceptance slice `Manifest Format`.
7. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
8. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Manifest Directory
     - Given: marketplace manifest is created
     - When: directory structure is determined
     - Then: path is .claude-plugin/marketplace.json
- [ ] Manifest Content
     - Given: manifest is created
     - When: content is populated
     - Then: JSON includes:
       - name: "NextLens Top-Down Bridge"
       - version: "1.0.0"
       - description: "Deterministic v1 top-down Feature packet bridge with validation and correction routing"
       - author: "NextLens Team"
       - repository: URL to GitHub repo
       - plugins: array of plugin definitions
       - keywords: array of searchable keywords
       - license: "MIT"
- [ ] Plugin Definitions
     - Given: plugins are defined
     - When: entries are populated
     - Then: each includes:
       - id: "nextlens-new" (or other command)
       - name: "NextLens New Packet"
       - description: clear summary
       - skills: array of skill file paths
- [ ] Skills Path References
     - Given: skill files are referenced
     - When: paths are verified
     - Then:
       - paths are relative to repository root
       - paths point to actual skill files
       - all referenced skills exist
- [ ] Manifest Format
     - Given: marketplace.json is created
     - When: format is validated
     - Then: JSON is valid and parseable

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 11.1](../stories/11.1-implement-module-identity-and-configuration-schema.md), [Story 11.2](../stories/11.2-implement-module-help-registration.md).
- Definition of done from the source plan:
  - .claude-plugin/marketplace.json is created
  - All required fields are present
  - Plugin definitions reference existing skills
  - JSON is valid

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

