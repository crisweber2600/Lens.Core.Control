---
feature: nextlens-src-implement
story_id: "1.1"
doc_type: story
status: ready
title: "Implement Command Entry Point and Argument Parser"
epic: "EP1: Command Spine & Interactive Entry Point"
priority: Critical (blocks all others)
complexity: Medium
depends_on: []
updated_at: 2026-05-14T22:22:48Z
---

# Story 1.1: Implement Command Entry Point and Argument Parser

## Context

As a **NextLens operator**, I want the system to **accept command-line arguments for mode (new, doctor, salmon), context input source, and optional config overrides**, so that **I can flexibly invoke different operational paths**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
This story has no upstream story dependencies.

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Command Entry Point and Argument Parser`.
2. Implement the acceptance slice `Command Parsing - New Mode`.
3. Implement the acceptance slice `Command Parsing - Doctor Mode`.
4. Implement the acceptance slice `Command Parsing - Salmon Mode`.
5. Implement the acceptance slice `Invalid Arguments`.
6. Implement the acceptance slice `Config Overrides`.
7. Implement the acceptance slice `Help Text`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Command Parsing - New Mode
     - Given: `nextlens new --context path/to/context.yaml`
     - When: command is parsed
     - Then: mode=`new`, context_source resolves to `path/to/context.yaml`, command proceeds to intake stage
- [ ] Command Parsing - Doctor Mode
     - Given: `nextlens doctor --packet path/to/packet.json`
     - When: command is parsed
     - Then: mode=`doctor`, packet_source resolves to `path/to/packet.json`, doctor validation stage starts
- [ ] Command Parsing - Salmon Mode
     - Given: `nextlens salmon --findings path/to/findings.jsonl`
     - When: command is parsed
     - Then: mode=`salmon`, findings_source resolves, correction routing stage starts
- [ ] Invalid Arguments
     - Given: invalid or missing required arguments
     - When: command is parsed
     - Then: usage/help message is printed with examples and valid next actions
- [ ] Config Overrides
     - Given: `nextlens new --context path/to/context.yaml --docs-path /custom/path`
     - When: command is parsed
     - Then: override variables are recorded; defaults are preserved for unspecified options
- [ ] Help Text
     - Given: `nextlens help` or `nextlens --help`
     - When: help is requested
     - Then: usage, modes, and example commands are printed

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Definition of done from the source plan:
  - Command parser accepts all three modes (new, doctor, salmon)
  - Invalid arguments produce helpful error messages
  - Help text is clear and includes examples
  - Config overrides are properly applied
  - Unit tests cover all parsing paths

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

