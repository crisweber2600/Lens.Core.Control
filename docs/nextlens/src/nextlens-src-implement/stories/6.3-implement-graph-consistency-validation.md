---
feature: nextlens-src-implement
story_id: "6.3"
doc_type: story
status: ready
title: "Implement Graph Consistency Validation"
epic: "EP6: Derived Graph Projection"
priority: High
complexity: Medium
depends_on: ["3.3", "6.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 6.3: Implement Graph Consistency Validation

## Context

As a **NextLens system**, I want to **validate that derived graph is consistent with authoritative state before packet emission**, so that **packet never references stale relationships**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md), [Story 6.2](../stories/6.2-implement-eager-graph-rebuild.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Graph Consistency Validation`.
2. Implement the acceptance slice `Entity Validation`.
3. Implement the acceptance slice `Edge Validation`.
4. Implement the acceptance slice `Checksum Validation`.
5. Implement the acceptance slice `Broken Reference Detection`.
6. Implement the acceptance slice `Orphaned Node Detection`.
7. Implement the acceptance slice `Check Result`.
8. Implement the acceptance slice `Recovery Guidance`.
9. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
10. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Entity Validation
     - Given: packet emission is about to start
     - When: consistency check runs
     - Then: system verifies that all entities referenced in graph exist in landscape state
- [ ] Edge Validation
     - Given: consistency check runs
     - When: edges are validated
     - Then: all edges reference valid nodes; no edges to non-existent nodes
- [ ] Checksum Validation
     - Given: consistency check runs
     - When: consistency checksum is compared
     - Then: checksum matches current state (no stale data)
- [ ] Broken Reference Detection
     - Given: consistency check runs
     - When: broken references are found
     - Then: check reports all broken references
- [ ] Orphaned Node Detection
     - Given: consistency check runs
     - When: orphaned nodes are found
     - Then: check reports all orphaned nodes
- [ ] Check Result
     - Given: consistency check completes
     - When: status is set
     - Then:
       - pass: all checks pass, emit packet
       - advisory: warnings exist but packet can emit with advisory
       - fail: critical issues, emit is blocked, landscape is checked for corruption
- [ ] Recovery Guidance
     - Given: consistency check fails
     - When: failure is reported
     - Then: error message identifies inconsistency and suggests recovery action (rebuild graph, fix landscape state, etc.)

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md), [Story 6.2](../stories/6.2-implement-eager-graph-rebuild.md).
- Definition of done from the source plan:
  - Entity validation is implemented
  - Edge validation is implemented
  - Checksum validation is implemented
  - Recovery guidance is provided
  - Integration test verifies consistency check before emission

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

