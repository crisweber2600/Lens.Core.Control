---
feature: nextlens-src-implement
story_id: "7.2"
doc_type: story
status: done
title: "Implement Core Doctor Checks"
epic: "EP7: Doctor Checks & Validation"
priority: High
complexity: High
depends_on: ["3.2", "6.3", "7.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 7.2: Implement Core Doctor Checks

## Context

As a **NextLens system**, I want to **provide essential doctor checks for common failure modes**, so that **operators can catch issues before packet emission**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 3.2](../stories/3.2-implement-landscape-state-persistence.md), [Story 6.3](../stories/6.3-implement-graph-consistency-validation.md), [Story 7.1](../stories/7.1-implement-doctor-check-framework.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Core Doctor Checks`.
2. Implement the acceptance slice `Check: schema-validity`.
3. Implement the acceptance slice `Check: feature-scope`.
4. Implement the acceptance slice `Check: traceability`.
5. Implement the acceptance slice `Check: context-readiness`.
6. Implement the acceptance slice `Check: write-boundary`.
7. Implement the acceptance slice `Check: graph-consistency`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Check: schema-validity
     - Given: landscape state is available
     - When: check runs
     - Then: check verifies:
       - all entity files match schema
       - required fields are present in all entities
       - no orphaned files exist
     - Severity: blocking
     - Result: pass/fail
- [ ] Check: feature-scope
     - Given: candidate Feature is selected
     - When: check runs
     - Then: check verifies:
       - selectedFeature.includedScope does not contain adjacent journeys
       - selectedFeature.includedScope does not contain future Features
       - selectedFeature.includedScope does not contain unrelated platform architecture
       - selectedFeature.explicitOutOfScope is populated
     - Severity: blocking (if spillage detected)
     - Result: pass/fail
- [ ] Check: traceability
     - Given: Feature packet is prepared
     - When: check runs
     - Then: check verifies:
       - packet.trace.systemId is valid and resolvable
       - packet.trace.roleIds all resolve to landscape roles
       - packet.trace.outcomeIds all resolve to landscape outcomes
       - packet.trace.journeyIds all resolve to landscape journeys
       - packet.selectionRationale is non-empty
     - Severity: advisory (if references broken)
     - Result: pass/warning
- [ ] Check: context-readiness
     - Given: context sufficiency passed
     - When: check runs
     - Then: check verifies:
       - BMAD consumer hints (prdInput, uxInput, architectureInput) are populated
       - top-down context includes system thesis
       - roles, outcomes, journeys are present
       - open questions and risks are captured
     - Severity: advisory
     - Result: pass/warning
- [ ] Check: write-boundary
     - Given: all pre-conditions checked
     - When: check runs
     - Then: check verifies:
       - write targets are within {docs_path}
       - write targets are not in governance repo
       - write targets are not in release clone paths
     - Severity: blocking
     - Result: pass/fail
- [ ] Check: graph-consistency
     - Given: derived graph is available
     - When: check runs
     - Then: check verifies:
       - derived graph is consistent with landscape state
       - consistency checksum matches current state
       - no broken references or orphaned nodes
     - Severity: advisory
     - Result: pass/warning

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 3.2](../stories/3.2-implement-landscape-state-persistence.md), [Story 6.3](../stories/6.3-implement-graph-consistency-validation.md), [Story 7.1](../stories/7.1-implement-doctor-check-framework.md).
- Definition of done from the source plan:
  - All six checks are implemented
  - Checks are non-mutating and read-only
  - Results follow standard format
  - Unit tests verify each check
  - Integration test verifies all checks execute correctly

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

