---
feature: nextlens-src-implement
story_id: "6.2"
doc_type: story
status: ready
title: "Implement Eager Graph Rebuild"
epic: "EP6: Derived Graph Projection"
priority: Critical
complexity: High
depends_on: ["3.2", "5.1", "6.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 6.2: Implement Eager Graph Rebuild

## Context

As a **NextLens system**, I want to **rebuild derived graph immediately after every successful authoritative write**, so that **graph is always consistent with latest state**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 3.2](../stories/3.2-implement-landscape-state-persistence.md), [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md), [Story 6.1](../stories/6.1-implement-derived-graph-schema.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Eager Graph Rebuild`.
2. Implement the acceptance slice `Post-Write Trigger`.
3. Implement the acceptance slice `Edge Resolution`.
4. Implement the acceptance slice `Broken Reference Detection`.
5. Implement the acceptance slice `Orphaned Node Detection`.
6. Implement the acceptance slice `Graph Output`.
7. Implement the acceptance slice `Consistency Checksum`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Post-Write Trigger
     - Given: landscape write completes successfully
     - When: post-write stage runs
     - Then: graph rebuild is triggered automatically (not optional)
- [ ] Edge Resolution
     - Given: graph rebuild runs
     - When: rebuilding edges
     - Then: all entity references are resolved:
       - system → role edges created for all roles in landscape
       - role → outcome edges created for roles that achieve outcomes
       - (and so on for all edge types)
- [ ] Broken Reference Detection
     - Given: graph rebuild runs
     - When: edge resolution occurs
     - Then: broken references are reported as warnings:
       - feature references non-existent journey
       - outcome references deleted role
       - (etc.)
- [ ] Orphaned Node Detection
     - Given: graph rebuild runs
     - When: node analysis occurs
     - Then: orphaned nodes are flagged:
       - nodes with no incoming or outgoing edges
       - nodes not reachable from system root
- [ ] Graph Output
     - Given: graph rebuild completes
     - When: output is written
     - Then: graph is persisted at {docs_path}/derived/graph.json with:
       - nodes array (id, type, label, metadata)
       - edges array (source, target, type, label, metadata)
       - metadata (generatedAt, sourceStateRef, consistency checksum)
- [ ] Consistency Checksum
      - Given: consistency checksum is generated
      - When: graph validation runs
      - Then: checksum is deterministic (same state → same checksum)
      - Checksum algorithm: SHA256(sorted node IDs + sorted edge IDs)

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 3.2](../stories/3.2-implement-landscape-state-persistence.md), [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md), [Story 6.1](../stories/6.1-implement-derived-graph-schema.md).
- Definition of done from the source plan:
  - Post-write rebuild is triggered automatically
  - All edges are resolved correctly
  - Broken references and orphaned nodes are detected
  - Graph is written with correct structure
  - Consistency checksum is deterministic
  - Integration test verifies rebuild on landscape write

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

