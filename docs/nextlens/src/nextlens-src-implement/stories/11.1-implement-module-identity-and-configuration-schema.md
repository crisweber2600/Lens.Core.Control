---
feature: nextlens-src-implement
story_id: "11.1"
doc_type: story
status: ready
title: "Implement Module Identity and Configuration Schema"
epic: "EP11: BMAD Module Packaging & Registration"
priority: High
complexity: Medium
depends_on: []
updated_at: 2026-05-14T22:22:48Z
---

# Story 11.1: Implement Module Identity and Configuration Schema

## Context

As a **NextLens module designer**, I want to **define module.yaml with NextLens identity and configuration variables**, so that **BMAD can discover and configure module capabilities**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
This story has no upstream story dependencies.

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Module Identity and Configuration Schema`.
2. Implement the acceptance slice `Module Identity`.
3. Implement the acceptance slice `Capabilities Definition`.
4. Implement the acceptance slice `Configuration Variables`.
5. Implement the acceptance slice `Dependencies`.
6. Implement the acceptance slice `Module YAML Format`.
7. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
8. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Module Identity
     - Given: module.yaml is created
     - When: identity fields are populated
     - Then: includes:
       - module_id: "nextlens-src" (lowercase, hyphenated)
       - module_name: "NextLens Top-Down Bridge" (human-readable)
       - module_version: "1.0.0" (semantic versioning)
       - description: clear summary of module purpose
       - author: "NextLens Team"
       - license: "MIT"
- [ ] Capabilities Definition
     - Given: module capabilities are defined
     - When: capabilities section is populated
     - Then: includes entries for:
       - command: nextlens-new
         - description: "Create one Feature packet from top-down discovery context"
         - entry_point: relative path to skill file
         - skill_type: "command"
       - command: nextlens-doctor
         - description: "Run non-mutating validation checks on packet or landscape"
         - entry_point: relative path to skill file
         - skill_type: "command"
       - command: nextlens-salmon
         - description: "Route correction signals through deduplication and impact classification"
         - entry_point: relative path to skill file
         - skill_type: "command"
- [ ] Configuration Variables
     - Given: module configuration is defined
     - When: configuration section is populated
     - Then: includes entries for:
       - NEXTLENS_DOCS_PATH: string, required, resolved from feature.yaml
       - NEXTLENS_LANDSCAPE_STORE: string, default {docs_path}/landscape
       - NEXTLENS_IDEMPOTENCY_TTL_HOURS: number, default 24
- [ ] Dependencies
     - Given: module dependencies are defined
     - When: dependencies section is populated
     - Then: includes:
       - feature-yaml-resolver (for docs path resolution)
       - bmad-constitution-resolver (for governance context)
- [ ] Module YAML Format
     - Given: module.yaml is created
     - When: format is validated
     - Then: file is valid YAML with correct schema

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Definition of done from the source plan:
  - module.yaml is created with all required fields
  - Schema is valid YAML
  - All capabilities are listed
  - Configuration variables are documented

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

