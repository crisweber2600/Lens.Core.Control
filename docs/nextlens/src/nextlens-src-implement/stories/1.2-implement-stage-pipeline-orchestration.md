---
feature: nextlens-src-implement
story_id: "1.2"
doc_type: story
status: ready
title: "Implement Stage Pipeline Orchestration"
epic: "EP1: Command Spine & Interactive Entry Point"
priority: Critical
complexity: High
depends_on: ["1.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 1.2: Implement Stage Pipeline Orchestration

## Context

As a **NextLens system architect**, I want the command spine **to transition deterministically through stages (intake → sufficiency → rank → confirm → write → rebuild → validate → emit → route)**, so that **operators always know which stage they're in and what comes next**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 1.1](../stories/1.1-implement-command-entry-point-and-argument-parser.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Stage Pipeline Orchestration`.
2. Implement the acceptance slice `Stage Pipeline Execution`.
3. Implement the acceptance slice `Stage Blocking`.
4. Implement the acceptance slice `Stage Transition Logging`.
5. Implement the acceptance slice `Error Recovery Context`.
6. Implement the acceptance slice `Stage State Persistence`.
7. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
8. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Stage Pipeline Execution
     - Given: command enters `new` mode
     - When: stages execute in sequence
     - Then: each stage prints `[stage:stageName]` header, processes input, prints outcome (pass/warning/fail)
- [ ] Stage Blocking
     - Given: a stage fails with blocking reason
     - When: the stage prints fail outcome
     - Then: next valid action is displayed and execution stops; no further stages run
- [ ] Stage Transition Logging
     - Given: a stage completes successfully
     - When: next stage is ready to start
     - Then: stage transition is logged with timestamp in evidence bundle
- [ ] Error Recovery Context
     - Given: execution is blocked at a stage
     - When: diagnostic context is needed
     - Then: context is preserved for resume without re-running prior stages
- [ ] Stage State Persistence
     - Given: pipeline is interrupted (user cancels, timeout, error)
     - When: interruption occurs
     - Then: pipeline state is saved to {docs_path}/.nextlens/pipeline-state.yaml for resume

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 1.1](../stories/1.1-implement-command-entry-point-and-argument-parser.md).
- Definition of done from the source plan:
  - All 9 stages execute in correct sequence
  - Stage transitions are logged with timestamps
  - Blocking conditions stop further execution
  - Evidence bundle records stage sequence
  - Error recovery context is preserved
  - Integration tests cover full pipeline

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

