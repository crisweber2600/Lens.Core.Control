---
feature: nextlens-src-implement
story_id: "10.2"
doc_type: story
status: done
title: "Implement Evidence Bundle Generation"
epic: "EP10: Evidence Bundle & Traceability"
priority: High
complexity: High
depends_on: ["10.1"]
updated_at: 2026-05-15T01:20:18Z
---

# Story 10.2: Implement Evidence Bundle Generation

## Context

As a **NextLens system**, I want to **emit evidence bundle as structured YAML with all stage records**, so that **bundle serves as complete run audit trail**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 10.1](../stories/10.1-implement-evidence-collection-framework.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Evidence Bundle Generation`.
2. Implement the acceptance slice `Bundle File Creation`.
3. Implement the acceptance slice `Run Metadata`.
4. Implement the acceptance slice `Context Intake Record`.
5. Implement the acceptance slice `Context Sufficiency Record`.
6. Implement the acceptance slice `Landscape State Record`.
7. Implement the acceptance slice `Feature Ranking Record`.
8. Implement the acceptance slice `Doctor Validation Record`.
9. Implement the acceptance slice `Graph Consistency Record`.
10. Implement the acceptance slice `Packet Emission Record`.
11. Implement the acceptance slice `Salmon Routing Record`.
12. Implement the acceptance slice `Artifacts Record`.
13. Implement the acceptance slice `Operator Confirmations Record`.
14. Implement the acceptance slice `Errors and Warnings Record`.
15. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
16. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Bundle File Creation
     - Given: run completes (success or failure)
     - When: bundle generation runs
     - Then: bundle is written to {docs_path}/.nextlens/evidence-{packetId-or-runId}.yaml, using run_id as the filename fallback when packet_id is null
- [ ] Run Metadata
     - Given: bundle is created
     - When: metadata is populated
     - Then: includes:
       - run_id: unique UUID
       - packet_id: packet ID if emitted, null if not
       - started_at: ISO 8601 timestamp
       - completed_at: ISO 8601 timestamp
       - duration_seconds: elapsed time
- [ ] Context Intake Record
     - Given: context intake stage completes
     - When: record is added to bundle
     - Then: record includes:
       - status: pass|fail
       - context_loaded_from: file path
       - schema_version_detected: version string
       - context_entity_counts: {systems, roles, outcomes, journeys, ...}
- [ ] Context Sufficiency Record
     - Given: sufficiency check completes
     - When: record is added to bundle
     - Then: record includes:
       - status: ready|ready_with_warnings|blocked
       - gate_results: [{gate_name, status, value_if_applicable}]
       - warnings: array of warning messages
       - recommendation: string
- [ ] Landscape State Record
     - Given: landscape stage completes
     - When: record is added to bundle
     - Then: record includes:
       - status: pass|fail
       - entities_loaded: {system: count, role: count, ...}
       - write_attempted: yes|no
       - write_status: success|failed|skipped
       - write_path: path if written
- [ ] Feature Ranking Record
     - Given: ranking stage completes
     - When: record is added to bundle
     - Then: record includes:
       - status: pass|fail
       - candidates_evaluated: count
       - top_candidate_selected: {id, name, score}
       - tie_break_applied: yes|no
       - tie_break_sequence: array of tiers applied
       - confirmation_given: yes|no
- [ ] Doctor Validation Record
     - Given: doctor stage completes
     - When: record is added to bundle
     - Then: record includes:
       - status: pass|fail
       - checks_run: count
       - blocking_findings: count
       - advisory_findings: count
       - informational_findings: count
       - doctor_report_path: file path
- [ ] Graph Consistency Record
     - Given: graph check completes
     - When: record is added to bundle
     - Then: record includes:
       - status: pass|fail
       - consistency_checksum: SHA256 hash
       - nodes_validated: count
       - edges_validated: count
- [ ] Packet Emission Record
     - Given: emission stage completes
     - When: record is added to bundle
     - Then: record includes:
       - status: success|skipped|failed
       - packet_id: UUID if emitted
       - packet_path: file path if written
       - idempotency_token: token used
       - idempotency_decision: new|replayed
- [ ] Salmon Routing Record
      - Given: salmon stage completes
      - When: record is added to bundle
      - Then: record includes:
        - events_created: count
        - events_merged: count
        - duplicates_ignored: count
        - event_summary_path: file path
- [ ] Artifacts Record
      - Given: bundle is created
      - When: artifacts section is populated
      - Then: includes paths to:
        - packet JSON file
        - doctor report JSONL
        - evidence bundle YAML (itself)
        - salmon events directory
        - landscape state directory
        - derived graph JSON
- [ ] Operator Confirmations Record
      - Given: operator makes confirmations
      - When: confirmations are recorded
      - Then: includes array of:
        - stage name
        - confirmation (yes|no|timeout)
        - timestamp
- [ ] Errors and Warnings Record
      - Given: run encounters errors or warnings
      - When: records are added
      - Then: includes:
        - error logs (if any)
        - warning logs (if any)
        - exception traces (if applicable)

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 10.1](../stories/10.1-implement-evidence-collection-framework.md).
- Definition of done from the source plan:
  - Bundle file is created at correct path
  - All required sections are populated
  - Bundle is valid YAML
  - Integration test verifies bundle generation

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

