---
feature: nextlens-src-implement
story_id: "6.1"
doc_type: story
status: ready
title: "Implement Derived Graph Schema"
epic: "EP6: Derived Graph Projection"
priority: High
complexity: Medium
depends_on: ["3.1", "3.3"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 6.1: Implement Derived Graph Schema

## Context

As a **NextLens system**, I want to **define a canonical derived graph schema that captures all landscape entity relationships**, so that **graph queries are fast and consistent**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 3.1](../stories/3.1-implement-stable-id-generation-and-validation.md), [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Derived Graph Schema`.
2. Implement the acceptance slice `Node Types`.
3. Implement the acceptance slice `Edge Types`.
4. Implement the acceptance slice `Node Properties`.
5. Implement the acceptance slice `Edge Properties`.
6. Implement the acceptance slice `Schema Versioning`.
7. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
8. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Node Types
     - Given: graph schema is defined
     - When: node types are specified
     - Then: schema includes nodes for:
       - system (root)
       - role
       - outcome
       - journey
       - operating_loop
       - capability
       - decision
       - risk
- [ ] Edge Types
     - Given: schema is defined
     - When: edge types are specified
     - Then: edges capture:
       - system → role (system requires role participation)
       - role → outcome (role achieves outcome)
       - outcome → journey (outcome fulfilled by journey)
       - journey → operating_loop (journey contains operating loop)
       - operating_loop → capability (loop uses capability)
       - capability → decision (decision governs capability)
       - feature → journey/outcome (feature proves journey/outcome)
- [ ] Node Properties
     - Given: node is defined
     - When: properties are specified
     - Then: node includes:
       - id (stable ID)
       - type (enum of node types)
       - label (human-readable name)
       - metadata (entity-specific fields)
- [ ] Edge Properties
     - Given: edge is defined
     - When: properties are specified
     - Then: edge includes:
       - source (node ID)
       - target (node ID)
       - type (edge type)
       - label (relationship description)
       - metadata (edge-specific fields)
- [ ] Schema Versioning
     - Given: schema is versioned
     - When: version is specified
     - Then: schema includes version identifier (e.g., "1.0")

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 3.1](../stories/3.1-implement-stable-id-generation-and-validation.md), [Story 3.3](../stories/3.3-implement-state-reconstruction-and-query.md).
- Definition of done from the source plan:
  - Node and edge types are defined
  - Schema is documented
  - Unit tests verify schema structure

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

