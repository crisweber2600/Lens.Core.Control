---
feature: nextlens-src-implement
story_id: "10.3"
doc_type: story
status: ready
title: "Implement Run Traceability Artifacts"
epic: "EP10: Evidence Bundle & Traceability"
priority: High
complexity: Medium
depends_on: ["8.4", "10.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 10.3: Implement Run Traceability Artifacts

## Context

As a **NextLens system**, I want to **generate a traceability index that links packet to evidence and source context**, so that **reviewers can quickly navigate audit artifacts**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 8.4](../stories/8.4-implement-packet-emission-and-file-writing.md), [Story 10.2](../stories/10.2-implement-evidence-bundle-generation.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Run Traceability Artifacts`.
2. Implement the acceptance slice `Traceability Index File`.
3. Implement the acceptance slice `Recent Runs Table`.
4. Implement the acceptance slice `Quick Links Section`.
5. Implement the acceptance slice `Lineage Section`.
6. Implement the acceptance slice `Key Decisions Section`.
7. Implement the acceptance slice `Index Updateability`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Traceability Index File
     - Given: packet is emitted and evidence is collected
     - When: traceability index is generated
     - Then: index is written to {docs_path}/.nextlens/traceability-index.md
- [ ] Recent Runs Table
     - Given: traceability index is created
     - When: recent runs section is populated
     - Then: table includes columns:
       - Run ID (first 8 chars)
       - Timestamp
       - Feature Selected
       - Doctor Status
       - Result (success|advisory|blocked)
       - Links to evidence and packet
- [ ] Quick Links Section
     - Given: index is created
     - When: quick links section is populated
     - Then: includes markdown links to:
       - Latest evidence bundle
       - Latest packet JSON
       - Latest doctor report
       - Latest salmon events summary
- [ ] Lineage Section
     - Given: index is created
     - When: lineage section is populated
     - Then: displays tree structure:
       ```
       System → Roles → Outcomes → Journeys → Feature → Packet
       [system-id]
         → [role-1-id]: [role-name]
             → [outcome-id]: [outcome-name]
                 → [journey-id]: [journey-name]
                     → [feature-id]: [feature-name]
                         → [packet-id]: [packet-path]
       ```
- [ ] Key Decisions Section
     - Given: index is created
     - When: key decisions section is populated
     - Then: includes:
       - Context sufficiency decision and rationale
       - Ranking scores and tie-break applied (if applicable)
       - Operator confirmations given
       - Doctor findings summary
- [ ] Index Updateability
     - Given: new runs are generated
     - When: traceability index is updated
     - Then:
       - new run entry is appended to recent runs table
       - latest links are updated
       - lineage is updated if different Feature selected

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 8.4](../stories/8.4-implement-packet-emission-and-file-writing.md), [Story 10.2](../stories/10.2-implement-evidence-bundle-generation.md).
- Definition of done from the source plan:
  - Traceability index file is created
  - All required sections are present
  - Links are accurate and resolvable
  - Index is human-readable markdown

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

