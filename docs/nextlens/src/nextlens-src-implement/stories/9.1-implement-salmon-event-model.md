---
feature: nextlens-src-implement
story_id: "9.1"
doc_type: story
status: ready
title: "Implement Salmon Event Model"
epic: "EP9: Salmon Correction Routing"
priority: High
complexity: Medium
depends_on: []
updated_at: 2026-05-14T22:22:48Z
---

# Story 9.1: Implement Salmon Event Model

## Context

As a **NextLens system**, I want to **define canonical Salmon event schema that captures correction signals from multiple sources**, so that **corrections are traceable and deduplicated**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
This story has no upstream story dependencies.

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Salmon Event Model`.
2. Implement the acceptance slice `Event Schema Definition`.
3. Implement the acceptance slice `Field Types`.
4. Implement the acceptance slice `Schema Validation`.
5. Implement the acceptance slice `Event Versioning`.
6. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
7. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Event Schema Definition
     - Given: Salmon event model is defined
     - When: schema is specified
     - Then: event includes:
       - id: unique event ID (UUID)
       - raisedFrom: source system (human, doctor check, reviewer, implementation)
       - source: {type: human|doctor|review|implementation, sourceId: check_id|reviewer_id|etc}
       - discovery: {issueDescription, impactedFeature, impactLevel}
       - impactedNodes: {features[], journeys[], outcomes[], roles[], operatingLoops[], capabilities[], bmadArtifacts[]}
       - severity: blocking|advisory|informational
       - recommendedAction: {type: local_note|landscape_update|block_packet|correct_course, details}
       - dedupFingerprint: deterministic hash
       - createdAt: ISO 8601 timestamp
       - routingResult: {status: created|merged|duplicate_ignored, targetRef: path or entity ID}
- [ ] Field Types
     - Given: event schema is defined
     - When: field types are specified
     - Then:
       - string fields are UTF-8, max 1000 chars (except description)
       - array fields are JSON arrays
       - timestamp fields are ISO 8601
       - hash fields are SHA256 hex
- [ ] Schema Validation
     - Given: event schema is defined
     - When: validation is implemented
     - Then: all required fields are checked before event creation
- [ ] Event Versioning
     - Given: event schema is defined
     - When: versioning is specified
     - Then: schema includes version identifier (e.g., "1.0")

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Definition of done from the source plan:
  - Event schema is fully defined
  - Schema validation is implemented
  - Schema is documented
  - Unit tests verify schema structure

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

