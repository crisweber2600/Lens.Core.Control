---
feature: nextlens-src-implement
story_id: "8.5"
doc_type: story
status: done
title: "Implement BMAD Scope Containment Enforcement"
epic: "EP8: Feature Packet Schema & Emission"
priority: High
complexity: Medium
depends_on: ["8.2"]
updated_at: 2026-05-15T01:06:08Z
---

# Story 8.5: Implement BMAD Scope Containment Enforcement

## Context

As a **NextLens system**, I want to **document and enforce constraints that BMAD must respect**, so that **BMAD does not expand scope beyond selected Feature**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 8.2](../stories/8.2-implement-packet-composition-logic.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement BMAD Scope Containment Enforcement`.
2. Implement the acceptance slice `Scope Constraints Documentation`.
3. Implement the acceptance slice `Selected Feature Scope`.
4. Implement the acceptance slice `Architecture Constraints`.
5. Implement the acceptance slice `Traceability Documentation`.
6. Implement the acceptance slice `Hint Format`.
7. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
8. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Scope Constraints Documentation
     - Given: Feature packet is emitted
     - When: bmadConsumerHints are populated
     - Then: hints include warning:
       ```
       "This packet represents one selected Feature from top-down discovery.
        Do not expand into adjacent journeys, future Features, platform
        architecture, or unrelated outcomes unless Salmon or correct-course
        signals scope change."
       ```
- [ ] Selected Feature Scope
     - Given: packet is prepared
     - When: bmadConsumerHints are populated
     - Then: hints include:
       - selectedFeature: {goal, includedScope, explicitOutOfScope}
- [ ] Architecture Constraints
     - Given: packet is prepared
     - When: bmadConsumerHints are populated
     - Then: hints include references to:
       - key architecture.md constraints affecting this Feature
       - feature packet schema version
       - packet composition timestamp
- [ ] Traceability Documentation
     - Given: packet is prepared
     - When: bmadConsumerHints are populated
     - Then: hints include lineage:
       ```
       system → role → outcome → journey → Feature
       ```
- [ ] Hint Format
     - Given: bmadConsumerHints are populated
     - When: format is determined
     - Then:
       - hints are human-readable text
       - references are resolvable (paths, IDs)
       - warnings are clear and actionable

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 8.2](../stories/8.2-implement-packet-composition-logic.md).
- Definition of done from the source plan:
  - Scope constraints are documented in packet
  - BMAD hints are clear and complete
  - Integration test verifies hints are present and readable

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

