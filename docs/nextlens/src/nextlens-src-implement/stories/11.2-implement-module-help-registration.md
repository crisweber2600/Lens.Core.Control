---
feature: nextlens-src-implement
story_id: "11.2"
doc_type: story
status: done
title: "Implement Module Help Registration"
epic: "EP11: BMAD Module Packaging & Registration"
priority: High
complexity: Low
depends_on: ["11.1"]
updated_at: 2026-05-15T01:26:21Z
---

# Story 11.2: Implement Module Help Registration

## Context

As a **NextLens module designer**, I want to **register capabilities in module-help.csv for BMAD discoverability**, so that **bmad-help can list and guide users**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 11.1](../stories/11.1-implement-module-identity-and-configuration-schema.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Module Help Registration`.
2. Implement the acceptance slice `CSV File Creation`.
3. Implement the acceptance slice `Command Registration`.
4. Implement the acceptance slice `Keyword Coverage`.
5. Implement the acceptance slice `CSV Format`.
6. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
7. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] CSV File Creation
     - Given: module-help.csv is created
     - When: format is determined
     - Then: file is CSV with columns:
       - command
       - category
       - description
       - entry_point
       - trigger_keywords
- [ ] Command Registration
     - Given: nextlens capabilities are listed
     - When: CSV rows are populated
     - Then: includes:
       ```
       nextlens-new,command,"Create one Feature packet from top-down discovery context",commands/new.ts,"nextlens new,top-down bridge,feature packet,deterministic selection"
       nextlens-doctor,command,"Run non-mutating validation checks on packet or landscape",commands/doctor.ts,"nextlens doctor,validate packet,check landscape,doctor validation"
       nextlens-salmon,command,"Route correction signals through deduplication and impact classification",commands/salmon.ts,"nextlens salmon,route correction,deduplicate events,correction routing"
       ```
- [ ] Keyword Coverage
     - Given: keywords are populated
     - When: bmad-help searches
     - Then: all relevant search terms are covered:
       - primary command name
       - feature name
       - key functionality
       - domain keywords
- [ ] CSV Format
     - Given: module-help.csv is created
     - When: format is validated
     - Then: file is valid CSV (RFC 4180 compliant)

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 11.1](../stories/11.1-implement-module-identity-and-configuration-schema.md).
- Definition of done from the source plan:
  - module-help.csv is created with correct format
  - All commands are registered
  - Keywords are comprehensive
  - bmad-help can parse and display entries

## Dev Agent Record

### Status
done

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [x] Acceptance criteria verified against implementation
- [x] Code review completed
- [x] Integration tests passing
- [x] Story marked complete

