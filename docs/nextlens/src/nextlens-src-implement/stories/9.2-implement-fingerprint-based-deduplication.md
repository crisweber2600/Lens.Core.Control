---
feature: nextlens-src-implement
story_id: "9.2"
doc_type: story
status: ready
title: "Implement Fingerprint-Based Deduplication"
epic: "EP9: Salmon Correction Routing"
priority: Critical
complexity: Medium
depends_on: ["9.1"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 9.2: Implement Fingerprint-Based Deduplication

## Context

As a **NextLens system**, I want to **deduplicate Salmon events using deterministic fingerprints**, so that **duplicate reports do not create duplicate events**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 9.1](../stories/9.1-implement-salmon-event-model.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Fingerprint-Based Deduplication`.
2. Implement the acceptance slice `Fingerprint Generation`.
3. Implement the acceptance slice `Fingerprint Storage Lookup`.
4. Implement the acceptance slice `New Event Path`.
5. Implement the acceptance slice `Duplicate Event Path`.
6. Implement the acceptance slice `Merged Event Path`.
7. Implement the acceptance slice `Fingerprint Determinism`.
8. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
9. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Fingerprint Generation
     - Given: new correction signal arrives
     - When: fingerprint is generated
     - Then: fingerprint is calculated as:
       - normalized_issue_class (standardized category name, lowercase)
       - target_stable_id (feature ID, journey ID, outcome ID, etc.)
       - canonical_path (normalized reference to affected node)
       - message_hash (SHA256 of normalized issue description)
       - fingerprint = SHA256(class + "|" + id + "|" + path + "|" + hash)
- [ ] Fingerprint Storage Lookup
     - Given: fingerprint is generated
     - When: deduplication check runs
     - Then: system checks if fingerprint exists in:
       - {docs_path}/.nextlens/salmon/fingerprints.json (index)
- [ ] New Event Path
     - Given: fingerprint does not exist
     - When: check completes
     - Then: event is marked as "new", routed to target, and persisted
- [ ] Duplicate Event Path
     - Given: fingerprint exists with same event recorded
     - When: check completes
     - Then: event is marked as "duplicate_ignored", evidence records provenance attachment
- [ ] Merged Event Path
     - Given: fingerprint exists but new source provides additional evidence
     - When: check completes
     - Then:
       - event is marked as "merged"
       - new source is appended to event.sources array
       - merged event is persisted (updates existing record)
- [ ] Fingerprint Determinism
     - Given: identical correction signals
     - When: fingerprint is generated twice
     - Then: fingerprints match exactly

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 9.1](../stories/9.1-implement-salmon-event-model.md).
- Definition of done from the source plan:
  - Fingerprint generation is deterministic
  - Lookup and deduplication logic works correctly
  - All three paths (new, duplicate, merged) are tested
  - Integration test verifies deduplication end-to-end

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

