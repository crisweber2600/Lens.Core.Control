---
feature: nextlens-src-implement
story_id: "5.3"
doc_type: story
status: ready
title: "Implement Response Replay Semantics"
epic: "EP5: Idempotent Writes & Replay Safety"
priority: High
complexity: Medium
depends_on: ["5.1", "5.2"]
updated_at: 2026-05-14T22:22:48Z
---

# Story 5.3: Implement Response Replay Semantics

## Context

As a **NextLens system**, I want to **return original response envelopes for replayed requests**, so that **operators experience consistent results across retries**.

Primary implementation root: `TargetProjects/nextlens/src/NextLens`.
Authoritative planning references live under `docs/nextlens/src/nextlens-src-implement`.
Blocked until the following stories are complete: [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md), [Story 5.2](../stories/5.2-implement-request-deduplication.md).

## Implementation Steps

1. Identify the smallest set of files under `TargetProjects/nextlens/src/NextLens` that directly control `Implement Response Replay Semantics`.
2. Implement the acceptance slice `Response Envelope Storage`.
3. Implement the acceptance slice `Response Replay`.
4. Implement the acceptance slice `Replay Logging`.
5. Implement the acceptance slice `Configuration Consistency`.
6. Implement the acceptance slice `Replay Idempotency`.
7. Add or update focused tests under `TargetProjects/nextlens/src/NextLens/tests` or adjacent test locations for the touched behavior.
8. Run the narrowest validation available before marking the story complete.

## Acceptance Criteria

- [ ] Response Envelope Storage
     - Given: operation completes successfully
     - When: response envelope is recorded
     - Then: envelope is stored in token record with:
       - operation_result (success/warning/failure)
       - output_summary (key values returned)
       - packet_reference (if applicable)
       - evidence_bundle_path (if applicable)
       - doctor_status (if applicable)
       - timestamp
- [ ] Response Replay
     - Given: idempotency token is marked completed with result stored
     - When: duplicate request with same token arrives
     - Then: original response envelope is returned without re-executing operation
- [ ] Replay Logging
     - Given: response is replayed
     - When: response is returned
     - Then: evidence bundle records that response was replayed, not freshly generated
- [ ] Configuration Consistency
     - Given: operator runs same command twice with different output configuration
     - When: idempotency token is identical
     - Then: the same result is returned (configuration override ignored for replay)
- [ ] Replay Idempotency
     - Given: same request is replayed multiple times
     - When: each replay is executed
     - Then: identical response envelope is returned each time

## Notes For Dev

- Implement code and tests in `TargetProjects/nextlens/src/NextLens`; do not place implementation code in control-repo docs paths.
- Planning source of truth: `docs/nextlens/src/nextlens-src-implement/stories.md`, `docs/nextlens/src/nextlens-src-implement/epics.md`, and `docs/nextlens/src/nextlens-src-implement/architecture.md`.
- Resolve dependencies first: [Story 5.1](../stories/5.1-implement-idempotency-token-generation-and-storage.md), [Story 5.2](../stories/5.2-implement-request-deduplication.md).
- Definition of done from the source plan:
  - Response envelope format is defined and stored
  - Replay logic returns original envelope
  - Replay logging is implemented
  - Integration test verifies replay consistency

## Dev Agent Record

### Status
ready-for-dev

### Agent Model Used
Claude Haiku 4.5

### Completion Notes List
- [ ] Acceptance criteria verified against implementation
- [ ] Code review completed
- [ ] Integration tests passing
- [ ] Story marked complete

